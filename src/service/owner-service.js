import { ROLES } from '../constant/Roles';
import API from './base-service';

export const OwnerService = {
    // getOfferByProperty: async function (status) {
    //     let result = await API.get(`/properties/${id}/offers`);
    //     return result.data;
    // }
}

export default OwnerService